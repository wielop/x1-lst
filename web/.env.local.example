NEXT_PUBLIC_RPC_URL=https://rpc.testnet.x1.xyz
NEXT_PUBLIC_PROGRAM_ID=5ViMkjJFgjUD9tuouTpjZ3m86jyGH8iB6h3r4Dxa4BCe
# Server-only (no NEXT_PUBLIC_ prefix) — read by app/api/reveal/route.ts,
# which proxies tile-reveal calls to the resolver. Kept server-side so the
# browser never talks to the resolver's plain-HTTP VPS address directly
# (that would get blocked as mixed content on an HTTPS page anyway).
RESOLVER_URL=http://localhost:8787
