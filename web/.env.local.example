NEXT_PUBLIC_RPC_URL=https://rpc.testnet.x1.xyz
NEXT_PUBLIC_PROGRAM_ID=5ViMkjJFgjUD9tuouTpjZ3m86jyGH8iB6h3r4Dxa4BCe
# Must be reachable from the *browser*, not just this machine. localhost
# only works when running the frontend locally alongside the resolver — a
# Vercel-hosted frontend needs the resolver exposed on a public URL.
NEXT_PUBLIC_RESOLVER_URL=http://localhost:8787
